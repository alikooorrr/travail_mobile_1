<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.ulaval.ima.tp1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ca.ulaval.ima.tp1.WebviewActivity"
            android:label="TP1"></activity>
        <activity
            android:name="ca.ulaval.ima.tp1.Activity4"
            android:label="Profil Description"></activity>
        <activity
            android:name="ca.ulaval.ima.tp1.ActivityBtnUlaval"
            android:label="ULavalActivity"></activity>
        <activity android:name="ca.ulaval.ima.tp1.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>